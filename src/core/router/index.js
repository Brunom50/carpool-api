
/**
 * Module dependencies.
 */

import Router from 'koa-router';

/**
 * `router`.
 */

const router = new Router();

/**
 * Register controllers.
 */

/**
 * Export `router`.
 */

export default router;
